<%= form_for(@viewmenu) do |f| %>
  <% if @viewmenu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@viewmenu.errors.count, "error") %> prohibited this viewmenu from being saved:</h2>

      <ul>
      <% @viewmenu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
