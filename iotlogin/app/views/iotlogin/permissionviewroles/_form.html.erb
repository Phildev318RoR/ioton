<%= form_for(@permissionviewrole) do |f| %>
  <% if @permissionviewrole.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@permissionviewrole.errors.count, "error") %> prohibited this permissionviewrole from being saved:</h2>

      <ul>
      <% @permissionviewrole.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :permissionview_id %><br>
    <%= f.number_field :permissionview_id %>
  </div>
  <div class="field">
    <%= f.label :role_id %><br>
    <%= f.number_field :role_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
