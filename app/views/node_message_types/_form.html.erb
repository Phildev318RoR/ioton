<%= form_for(@node_message_type) do |f| %>
  <% if @node_message_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@node_message_type.errors.count, "error") %> prohibited this node_message_type from being saved:</h2>

      <ul>
      <% @node_message_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :downloaded %><br>
    <%= f.check_box :downloaded %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
